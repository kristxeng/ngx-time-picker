import { NgxTimePickerDirective } from './ngx-time-picker.directive';

describe('NgxTimePickerDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxTimePickerDirective();
    expect(directive).toBeTruthy();
  });
});
