import { TimePickerPanelCtrlRef } from './time-picker-panel-ctrl-ref';

describe('TimePickerPanelCtrlRef', () => {
  it('should create an instance', () => {
    expect(new TimePickerPanelCtrlRef()).toBeTruthy();
  });
});
